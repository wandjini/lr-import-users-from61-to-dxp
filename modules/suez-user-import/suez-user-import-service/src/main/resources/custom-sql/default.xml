<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.suez.user.synch.service.persistence.impl.ExtUserFinder.findUsersByCompanyAndRole">
	<![CDATA[
		SELECT u.*
		FROM (user_ u 
				INNER JOIN 
				users_roles ur ON ( ur.userid = u.userid
					  				AND ur.roleid = ?
					  		      )
			)
		WHERE u.companyid = ?		
		]]>
	</sql>
	<sql id="com.liferay.suez.user.synch.service.persistence.impl.ExtUserFinder.countUsersByCompanyAndRole">
		<![CDATA[
			SELECT count(*)
			FROM (user_ u 
					INNER JOIN 
					users_roles ur ON ( ur.userid = u.userid
						  				AND ur.roleid = ? 
						  		
						  		   	   )
				)
			WHERE u.companyid = ?		
		]]>
	</sql>
</custom-sql>